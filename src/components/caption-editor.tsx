"use client";

export function CaptionEditor({
  vtId,
  currentCaption,
  prompt,
}: {
  vtId: string;
  currentCaption: string | null | undefined;
  prompt: string | null | undefined;
}) {
  return null; // Stub — will be implemented in Task 2
}
